{
  "name": "@potbot/asset-registry",
  "version": "0.1.0",
  "description": "PotBot Asset Registry — curated, verifiable Solana asset list + NAV/pricing services for Pots. Off-chain layer; does not widen the on-chain program attack surface.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --import tsx --test test/**/*.test.ts",
    "resolve-mints": "tsx scripts/resolve-mints.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "@types/node": "^20.14.0"
  }
}
