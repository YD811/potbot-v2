{
  "name": "@potbot/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "deprecated": "Replaced by apps/web Vercel routes — see DEPRECATED.md",
  "scripts": {
    "dev":   "echo 'apps/api is deprecated — see DEPRECATED.md' && exit 0",
    "build": "echo 'apps/api is deprecated — see DEPRECATED.md' && exit 0",
    "start": "echo 'apps/api is deprecated — see DEPRECATED.md' && exit 0",
    "lint":  "echo 'apps/api is deprecated — see DEPRECATED.md' && exit 0",
    "test":  "echo 'apps/api is deprecated — see DEPRECATED.md' && exit 0"
  },
  "dependencies": {
    "@hono/node-server":  "^1.12.0",
    "@supabase/supabase-js": "^2.45.0",
    "@solana/web3.js":    "^1.98.0",
    "bs58":               "^6.0.0",
    "hono":               "^4.5.0",
    "node-cron":          "^3.0.3",
    "dotenv":             "^16.4.5"
  },
  "devDependencies": {
    "@types/node":       "^22.0.0",
    "@types/node-cron":  "^3.0.11",
    "tsx":               "^4.16.2",
    "typescript":        "^5.5.3"
  }
}
