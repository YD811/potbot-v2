# PotBot MCP Server — copy to .env and fill in values

# Solana RPC
RPC_URL=https://api.devnet.solana.com

# Program
PROGRAM_ID=2ywztkP4gaJr2HtmBvqMXrBWab3FLd3uG6TjGXvVogJL

# PotBot API (backend)
API_URL=http://localhost:3001

# Optional: Agent keypair (base58 private key) for autonomous proposal creation
# WARNING: Keep this secret — never commit it
# AGENT_KEYPAIR=your-base58-private-key

# Optional: x402 micropayment config
# X402_ENABLED=false
# X402_FEE_USDC=0.001
